#include <iostream>

using namespace std;

const int inf = 0xf3f3f3;
int arr[100000], dp[100000];

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);

    int cnt = 0;
    while (true) {
        int n;
        cin >> n;
        cnt++;

        if (n == 0) break;
    }
}