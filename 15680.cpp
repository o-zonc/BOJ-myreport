#include <iostream>

using namespace std;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(0);

    int n;
    cin >> n;

    if (n) {
        cout << "Leading the Way to the Future";
    } else {
        cout << "YONSEI";
    }
}